{
  "name": "braid-web",
  "private": true,
  "version": "0.1.0",
  "description": "Browser build of braid: merge JW Library backups with no computer involved",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-timeout=30000 web/store.test.mjs web/i18n.test.mjs",
    "build": "python3 web/build.py"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
